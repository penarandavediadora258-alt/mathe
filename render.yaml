services:
  - type: web
    name: mathe-backend
    env: node
    branch: main
    rootDir: backend
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true

  - type: web
    name: mathe-frontend
    env: docker
    branch: main
    rootDir: frontend
    plan: starter
    dockerfilePath: Dockerfile
    autoDeploy: true
